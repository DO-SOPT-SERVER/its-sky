package org.example;

import org.domain.Child;
import org.domain.Poketmon;
import org.domain.PoketmonType;

import static org.domain.PoketmonType.*;

public class Main {
	public static void main(String[] args) {
		/*Child child = new Child();
		child.인사();*/

		/*Poketmon pikachu = new Poketmon("Pikachu", ELECTRIC);
		System.out.println("pikachu.name = " + pikachu.name); // name, type과 같은 필드에 직접적으로 접근하는 것은 객체 지향의 설계 원칙에 어긋남
		System.out.println("pikachu.type = " + pikachu.type); // 그래서 값을 얻고 싶을 때는 getter의 방식이나 불필요한 상황에서는 피해야함 */


	}
}